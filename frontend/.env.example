# Copy to .env for local development.
# Left unset (or "/api"), the dev server proxies /api to the backend
# (see vite.config.ts) so you don't have to think about CORS locally.
# In production, point this at your deployed API's base URL instead.
VITE_API_URL=/api
